import appleSignInButton from "./appleSignInButton.png";
import googleSignInButton from "./googleSignInButton.svg";

const images = {
  appleSignInButton: appleSignInButton,
  googleSignInButton: googleSignInButton,
};
export default images;
